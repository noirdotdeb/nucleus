// createElement(tag, props, children)
// Takes: a tag name, an object of attributes, an array of children
// Returns: a plain object describing what this element should look like
// (NOT a real DOM node — just data describing one)

export function createElement(tag, props, children) {
  // TODO: return an object shaped like { tag, props, children }
}g